import './inputForSettings2.css'

const CustomInputForSettings2 = (props) => {
  return (
      <div className="inputForSettings2">
        { props.role }
      </div>

  );
};

export default CustomInputForSettings2;